    kind: PersistentVolumeClaim
    apiVersion: v1
    metadata:
      name: nfs-wf-volume 
    spec:
      storageClassName: "nfs"
      accessModes:
        - ReadWriteMany
      resources:
        requests:
          storage: 5Gi
